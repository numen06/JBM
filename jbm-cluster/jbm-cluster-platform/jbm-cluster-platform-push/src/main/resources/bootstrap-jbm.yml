spring:
  cloud:
    nacos:
      config:
        namespace: jbm
        #        group: ${config.group}
        refreshable-dataids: common.properties
        server-addr: nacos:8848
        shared-dataids: common.properties,redis.properties,rabbitmq.properties,db.properties,mqtt.properties,sms.properties,mail.properties
      discovery:
        #        namespace:
        #        group: ${config.group}
        server-addr: nacos:8848
#        ip: 10.100.50.2