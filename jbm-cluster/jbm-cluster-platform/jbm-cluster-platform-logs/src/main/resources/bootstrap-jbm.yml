spring:
  cloud:
    nacos:
      config:
        namespace: jbm
        #        group: ${config.group}
        server-addr: nacos2:8848
      discovery:
        #        namespace:
        #        group: ${config.group}
        server-addr: nacos2:8848
#        ip: 10.100.50.2