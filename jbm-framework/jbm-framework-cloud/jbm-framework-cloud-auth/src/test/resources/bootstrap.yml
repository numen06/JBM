spring:
  application:
    name: hubao-platform-cloud-auth
  cloud:
    config:
      uri: http://platform-config.hubao-tech.com
      fail-fast: true
      username: hubao
      password: Hubao@20170604
      retry:
        initial-interval: 2000
        max-interval: 10000
        multiplier: 2
        max-attempts: 10
